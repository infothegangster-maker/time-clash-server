# Time Clash Server - Environment Setup

## Copy this file and rename to `.env` in production

PORT=3000
NODE_ENV=production

# JWT Secret - Generate your own using: openssl rand -base64 32
JWT_SECRET=timeclash_super_secret_jwt_key_make_it_very_long_and_random_2025

# Redis URL - Get from Upstash: https://console.upstash.com/
# Format: rediss://default:PASSWORD@HOST:6379
REDIS_URL=redis://localhost:6379

# CORS Origins - Set to your domain in production
CORS_ORIGIN=*

# Game Configuration
GAME_DURATION_MS=10000
WIN_TOLERANCE_MS=50
LATENCY_BUFFER_MS=200

